export default function HomePage() {
  return (
    <main style={{ padding: 24, fontFamily: "sans-serif" }}>
      <h1>wavy-app</h1>
      <p>Part 1 bootstrap is ready. Next step is DB schema + seed + matching endpoint.</p>
    </main>
  );
}
